"""Cash flow services.

`entry` records transactions and raises the probable-duplicate advisory.
`categories` maintains the taxonomy. `recurring` works out which proposals are
outstanding. `reporting` totals by category — per currency, and never
translated.
"""
